[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# /e/<id>  ->  event-preview function (id passed as query param)
[[redirects]]
  from = "/e/:id"
  to = "/.netlify/functions/event-preview?id=:id"
  status = 200
  force = true
